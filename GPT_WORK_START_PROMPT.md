# GPT WORK 시작 프롬프트

아래 내용을 새 GPT WORK 세션의 첫 메시지로 사용하세요.

---

나는 `ai_interview_coach`라는 AI 면접 코치 웹앱의 개발을 이어가려고 합니다. 이 프로젝트는 **React 19 + TypeScript + Tailwind CSS 4 + Express 4 + tRPC 11 + Drizzle ORM + MySQL/TiDB** 기반이며, Manus WebDev에서 운영 중인 애플리케이션을 GPT WORK에서 분석·개선하려는 목적입니다.

업로드한 `GPT_WORK_HANDOVER.md`와 소스 ZIP을 먼저 모두 읽어 주세요. **첫 단계에서는 파일을 수정하지 말고** 다음 표를 작성해 주세요.

| 진단 대상 | 확인할 내용 |
|---|---|
| 실행 환경 | Node 버전, pnpm, Python Edge-TTS, DB, S3, OAuth 필요 조건 |
| 구조 | 프론트, tRPC 서버, DB 스키마, PDF·TTS 모듈 간 호출 관계 |
| 기능 상태 | 구현 완료 기능과 아직 검증이 필요한 기능 |
| 보안 | `.env`, JWT, OAuth, S3, 결제 키가 노출되지 않았는지 |
| 이관 누락 | 폰트, 대용량 미디어, migration, 외부 서비스 설정의 누락 여부 |
| 검증 계획 | 타입 검사, 단위 테스트, 브라우저 흐름, 모바일·음성 검증 순서 |

진단할 때 비밀키를 출력하거나 새 키를 만들어 달라고 요구하지 마세요. 환경변수는 이름과 용도만 기록하고 값은 해당 환경의 Secret 관리 기능으로 별도 주입해야 합니다.

이 앱의 핵심 사용자 흐름은 다음과 같습니다.

1. 7단계 설정 위자드에서 기본정보, 이력서·자소서, 면접 단계, 모드, 면접관, 시간, 준비 완료를 순서대로 입력합니다.
2. 6명의 면접관 아바타가 각자의 voice ID와 스타일로 질문을 읽습니다.
3. 사용자가 답변을 녹음하거나 입력하고 STT 결과를 확인합니다.
4. 사용자가 **‘피드백 받기’ 버튼을 직접 눌러야** 답변 제출·분석·AI 모범 답안 생성이 시작됩니다.
5. 분석 중에는 ‘음성 분석 중’ 또는 ‘답변 분석 중’ → ‘답변 내용 분석 중’ → ‘모범 답안 생성 중’ → ‘피드백 정리 완료’가 순차적으로 보입니다.
6. 결과 페이지에서 STT 스크립트, 추임새·반복어 통계 및 하이라이트, 모범 답안 비교, 워드클라우드, PDF/Word export를 제공합니다.
7. 예상 질문 PDF는 카테고리와 개별 질문 체크박스를 모두 반영해야 합니다.

앞으로 코드를 수정할 때 다음 규칙을 지켜 주세요.

- 기존 tRPC 계약과 `protectedProcedure` 사용자 소유권 검증을 보존합니다.
- 프론트엔드에서 새 REST/fetch 계층을 임의로 만들지 않습니다.
- 사용자 데이터, 답변 녹음, 리포트 파일을 클라이언트 전용 메모리에만 두지 말고 기존 DB/S3 흐름을 확인합니다.
- `client/src/lib/transcriptAnalysis.ts`의 분석 결과와 `InterviewResult.tsx`의 하이라이트·통계 결과가 서로 달라지지 않게 합니다.
- `submitMutation.isPending` 동안 ‘피드백 받기’ 버튼 중복 제출을 막고, 실패 시 사용자가 입력한 답변을 보존합니다.
- 6명의 아바타 음성을 단일 voice ID로 통합하지 않습니다.
- CJK 폰트와 한글 PDF 출력을 검증합니다.
- 큰 이미지·오디오·폰트는 `client/public`에 무분별하게 넣지 않습니다.
- 변경 전에는 계획을 표로 설명하고, 변경 후에는 수정 파일, 테스트 결과, 남은 위험을 요약합니다.
- 테스트가 기존 DB 상태나 외부 음성 모듈 때문에 실패하면 신규 코드 오류와 환경 오류를 분리해 보고합니다.

첫 응답에서는 위 규칙에 따라 **진단 표와 이관 후 첫 작업 제안만** 작성하고 실제 파일 수정은 기다려 주세요.
